---
phase: 02-e-commerce-order-flow-wms
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - vitest.config.ts
  - src/__tests__/setup.ts
  - src/__tests__/cart/cart-store.test.ts
  - src/__tests__/checkout/checkout-flow.test.ts
  - src/__tests__/stripe/stripe-webhook.test.ts
  - src/__tests__/bacs/bacs-payment.test.ts
  - src/__tests__/allocation/fifo-allocation.test.ts
  - src/__tests__/picklist/picklist.test.ts
  - src/__tests__/dispatch/dispatch.test.ts
  - src/__tests__/certpack/certpack.test.ts
  - src/__tests__/email/email-service.test.ts
  - src/__tests__/orders/orders.test.ts
  - src/__tests__/invoice/invoice.test.ts
  - src/__tests__/stock/stock-adjustments.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "vitest runs successfully with zero tests (empty suite baseline)"
    - "All test stub files import vitest and compile without errors"
    - "Path aliases (@/) resolve correctly in test files"
    - "Drizzle and tRPC mock helpers are available in setup.ts"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest configuration with path aliases matching tsconfig"
      contains: "defineConfig"
    - path: "src/__tests__/setup.ts"
      provides: "Global test setup with Drizzle/tRPC mock helpers"
      contains: "mock"
    - path: "src/__tests__/cart/cart-store.test.ts"
      provides: "Cart store test stub"
    - path: "src/__tests__/allocation/fifo-allocation.test.ts"
      provides: "FIFO allocation test stub"
    - path: "src/__tests__/certpack/certpack.test.ts"
      provides: "Cert pack generation test stub"
  key_links:
    - from: "vitest.config.ts"
      to: "tsconfig.json"
      via: "Path alias resolution (@/ -> src/)"
      pattern: "resolve.*alias|@/"
---

<objective>
Create the vitest test infrastructure and test stub files for all Phase 2 requirements. Every subsequent plan's verify element depends on these test directories existing.

Purpose: Establishes the Nyquist-compliant feedback loop. Without this, all other plans can only verify via tsc --noEmit (type checking) with no functional feedback. Test stubs contain describe/it blocks with todo markers that subsequent plans fill in as they implement features.
Output: vitest.config.ts, test setup file, 11 test stub files across 11 directories.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-e-commerce-order-flow-wms/02-VALIDATION.md

@tsconfig.json
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install vitest and create configuration</name>
  <files>
    vitest.config.ts,
    src/__tests__/setup.ts
  </files>
  <action>
    Install vitest as a dev dependency: `npm install -D vitest @vitest/coverage-v8`.

    **vitest.config.ts:** Create vitest configuration.
    - Import defineConfig from vitest/config
    - Set resolve.alias to match tsconfig paths (e.g., "@/" -> "./src/")
    - Set test.globals: true (so describe/it/expect are available without import)
    - Set test.environment: "node" (server-side logic dominates Phase 2)
    - Set test.setupFiles: ["./src/__tests__/setup.ts"]
    - Set test.include: ["src/__tests__/**/*.test.ts"]
    - Set test.coverage.provider: "v8"
    - Set test.reporters: ["verbose"] as default

    **src/__tests__/setup.ts:** Global test setup.
    - Import vi from vitest
    - Create a mock helper for Drizzle database transactions:
      ```typescript
      export function createMockTx() {
        return {
          select: vi.fn().mockReturnThis(),
          insert: vi.fn().mockReturnThis(),
          update: vi.fn().mockReturnThis(),
          delete: vi.fn().mockReturnThis(),
          query: vi.fn().mockReturnThis(),
          from: vi.fn().mockReturnThis(),
          where: vi.fn().mockReturnThis(),
          values: vi.fn().mockReturnThis(),
          set: vi.fn().mockReturnThis(),
          returning: vi.fn().mockResolvedValue([]),
        };
      }
      ```
    - Create a mock helper for tRPC context:
      ```typescript
      export function createMockCtx(overrides?: Partial<{ userId: string; role: string }>) {
        return {
          session: { user: { id: overrides?.userId ?? "test-user-id" } },
          db: createMockTx(),
          ...overrides,
        };
      }
      ```
    - Mock the R2 storage module: vi.mock("@/server/storage", () => ({ ... }))
    - These helpers will be imported by individual test files as needed.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>vitest.config.ts exists with correct path aliases. setup.ts provides mock helpers. `npx vitest run` executes without errors (zero tests is OK at this stage).</done>
</task>

<task type="auto">
  <name>Task 2: Create all test stub files for Phase 2 requirements</name>
  <files>
    src/__tests__/cart/cart-store.test.ts,
    src/__tests__/checkout/checkout-flow.test.ts,
    src/__tests__/stripe/stripe-webhook.test.ts,
    src/__tests__/bacs/bacs-payment.test.ts,
    src/__tests__/allocation/fifo-allocation.test.ts,
    src/__tests__/picklist/picklist.test.ts,
    src/__tests__/dispatch/dispatch.test.ts,
    src/__tests__/certpack/certpack.test.ts,
    src/__tests__/email/email-service.test.ts,
    src/__tests__/orders/orders.test.ts,
    src/__tests__/invoice/invoice.test.ts,
    src/__tests__/stock/stock-adjustments.test.ts
  </files>
  <action>
    Create 12 test stub files. Each file should follow this pattern:
    - Import { describe, it, expect } from "vitest" (even with globals, explicit imports aid IDE)
    - One top-level describe block named after the feature
    - Individual it blocks with .todo() for each expected behavior
    - Comment at the top indicating which Plan fills in the tests

    **src/__tests__/cart/cart-store.test.ts:** (Plan 01 fills)
    ```typescript
    describe("Cart Store", () => {
      it.todo("adds item to empty cart");
      it.todo("increments quantity when adding existing item");
      it.todo("removes item from cart");
      it.todo("updates item quantity");
      it.todo("removes item when quantity set to 0");
      it.todo("clears all items");
      it.todo("computes total items count");
      it.todo("persists cart to localStorage");
    });
    ```

    **src/__tests__/checkout/checkout-flow.test.ts:** (Plan 07 fills)
    ```typescript
    describe("Checkout Flow", () => {
      it.todo("rejects checkout with empty cart");
      it.todo("validates delivery address is selected");
      it.todo("requires PO number for credit payment");
      it.todo("allows optional PO number for card/BACS");
      it.todo("creates order with server-side price validation");
    });
    ```

    **src/__tests__/stripe/stripe-webhook.test.ts:** (Plan 07 fills)
    ```typescript
    describe("Stripe Webhook", () => {
      it.todo("verifies webhook signature");
      it.todo("transitions order to confirmed on checkout.session.completed");
      it.todo("triggers FIFO allocation after payment confirmation");
      it.todo("skips already-confirmed orders (idempotency)");
      it.todo("returns 400 on invalid signature");
    });
    ```

    **src/__tests__/bacs/bacs-payment.test.ts:** (Plan 07 fills)
    ```typescript
    describe("BACS Payment", () => {
      it.todo("transitions order to awaiting_payment");
      it.todo("admin confirms BACS receipt transitions to confirmed");
      it.todo("generates proforma invoice with bank details");
    });
    ```

    **src/__tests__/allocation/fifo-allocation.test.ts:** (Plan 01 fills)
    ```typescript
    describe("FIFO Allocation", () => {
      it.todo("allocates from oldest batch first");
      it.todo("splits across batches when single batch insufficient");
      it.todo("rejects allocation when total stock insufficient");
      it.todo("transitions order to allocated after successful allocation");
    });
    ```

    **src/__tests__/picklist/picklist.test.ts:** (Plan 04 fills)
    ```typescript
    describe("Pick List", () => {
      it.todo("returns allocated batch IDs per order line");
      it.todo("shows FIFO-ordered batches (oldest first)");
      it.todo("transitions order from allocated to picking");
      it.todo("transitions order from picking to packed");
    });
    ```

    **src/__tests__/dispatch/dispatch.test.ts:** (Plan 04 fills)
    ```typescript
    describe("Dispatch", () => {
      it.todo("requires tracking number for parcel dispatch");
      it.todo("requires consignment number for pallet dispatch");
      it.todo("transitions packed to dispatched");
      it.todo("triggers cert pack generation on dispatch");
      it.todo("stores dispatch details on order");
    });
    ```

    **src/__tests__/certpack/certpack.test.ts:** (Plan 05 fills)
    ```typescript
    describe("Cert Pack Generation", () => {
      it.todo("generates PDF with cover page");
      it.todo("includes traceability table with batch refs");
      it.todo("appends original cert PDFs unaltered");
      it.todo("uploads cert pack to R2 and updates order");
      it.todo("handles missing cert PDF gracefully");
    });
    ```

    **src/__tests__/email/email-service.test.ts:** (Plan 06 fills)
    ```typescript
    describe("Email Service", () => {
      it.todo("sends order confirmation email via Resend");
      it.todo("sends dispatch notification with tracking info");
      it.todo("attaches cert pack when under 10MB");
      it.todo("includes download link when cert pack over 10MB");
      it.todo("does not throw on email failure (logs error)");
    });
    ```

    **src/__tests__/orders/orders.test.ts:** (Plan 03 + Plan 06 fills)
    ```typescript
    describe("Orders", () => {
      it.todo("creates order with server-side price from products.pricePence");
      it.todo("rejects order with unpriced product");
      it.todo("validates order status transitions");
      it.todo("returns user's own orders only");
      it.todo("returns order detail with batch allocations");
      it.todo("reorder returns current prices for order items");
    });
    ```

    **src/__tests__/invoice/invoice.test.ts:** (Plan 05 fills)
    ```typescript
    describe("Invoice Generation", () => {
      it.todo("generates invoice PDF with line items and VAT");
      it.todo("includes Torke batch ID per line item");
      it.todo("generates proforma with bank details for BACS");
      it.todo("creates invoice record in database");
      it.todo("uploads PDF to R2");
    });
    ```

    **src/__tests__/stock/stock-adjustments.test.ts:** (Plan 02 fills)
    ```typescript
    describe("Stock Adjustments", () => {
      it.todo("adjusts batch quantity with reason code");
      it.todo("rejects adjustment that would make quantity negative");
      it.todo("logs adjustment with user and timestamp");
      it.todo("returns stock dashboard with product summaries");
    });
    ```

    Every stub uses .todo() so vitest reports them as pending but does not fail. Plans fill in the actual test logic as they implement features.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>All 12 test stub files exist and compile. `npx vitest run` reports todo tests as pending with zero failures. Every test directory referenced in VALIDATION.md exists.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` completes with 0 failures
- `npx tsc --noEmit` passes (test files compile)
- All 11 directories exist under src/__tests__/
- vitest.config.ts resolves @/ path alias
</verification>

<success_criteria>
1. vitest installed and configured with path aliases matching tsconfig
2. Test setup provides Drizzle and tRPC mock helpers
3. All 12 test stub files exist with todo tests for Phase 2 requirements
4. `npx vitest run` executes successfully (zero failures, pending todos)
5. Every test directory in VALIDATION.md Wave 0 Requirements is created
</success_criteria>

<output>
After completion, create `.planning/phases/02-e-commerce-order-flow-wms/02-00-SUMMARY.md`
</output>
