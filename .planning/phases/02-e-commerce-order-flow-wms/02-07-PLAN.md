---
phase: 02-e-commerce-order-flow-wms
plan: 07
type: execute
wave: 3
depends_on: ["02-00", "02-03"]
files_modified:
  - src/app/(shop)/checkout/page.tsx
  - src/components/shop/CheckoutWizard.tsx
  - src/components/shop/CheckoutStep1Address.tsx
  - src/components/shop/CheckoutStep2Payment.tsx
  - src/components/shop/CheckoutStep3Review.tsx
  - src/app/(shop)/checkout/success/page.tsx
  - src/server/services/payment-service.ts
  - src/app/api/stripe/webhook/route.ts
  - src/app/(wms)/credit-accounts/page.tsx
  - src/components/wms/CreditAccountManager.tsx
autonomous: true
requirements: [SHOP-08, SHOP-09, SHOP-10, TRACE-07]
user_setup:
  - service: stripe
    why: "Card payment processing with SCA compliance"
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: "Stripe Dashboard -> Developers -> API keys -> Secret key (use test mode key sk_test_...)"
      - name: STRIPE_WEBHOOK_SECRET
        source: "Run 'stripe listen --forward-to localhost:3000/api/stripe/webhook' and copy the webhook signing secret (whsec_...)"
      - name: NEXT_PUBLIC_APP_URL
        source: "Set to http://localhost:3000 for development"
  - service: resend
    why: "Transactional email delivery (order confirmation, dispatch notification)"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys -> Create API Key"

must_haves:
  truths:
    - "Customer can complete checkout with PO number entry"
    - "Customer can pay by card via Stripe redirect"
    - "Customer can pay on credit terms if account is approved"
    - "Customer can place a BACS order and see bank details on success page"
    - "Order confirmation shows batch allocation per line item"
  artifacts:
    - path: "src/app/(shop)/checkout/page.tsx"
      provides: "Multi-step checkout wizard"
    - path: "src/server/services/payment-service.ts"
      provides: "Stripe session creation, credit validation, BACS handling"
      exports: ["createStripeCheckoutSession", "validateCreditAccount", "processPayment"]
    - path: "src/app/api/stripe/webhook/route.ts"
      provides: "Stripe webhook handler for payment confirmation"
    - path: "src/app/(shop)/checkout/success/page.tsx"
      provides: "Post-checkout success page with batch allocations and BACS bank details"
    - path: "src/app/(wms)/credit-accounts/page.tsx"
      provides: "Admin credit account management"
  key_links:
    - from: "src/components/shop/CheckoutStep2Payment.tsx"
      to: "src/server/services/payment-service.ts"
      via: "tRPC mutation calling processPayment"
      pattern: "processPayment|createStripeCheckoutSession"
    - from: "src/app/api/stripe/webhook/route.ts"
      to: "src/server/services/order-service.ts"
      via: "transitionOrder on payment_intent.succeeded"
      pattern: "transitionOrder.*confirmed"
    - from: "src/app/(shop)/checkout/success/page.tsx"
      to: "src/server/services/batch-service.ts"
      via: "allocateOrderStock shows FIFOAllocation results"
      pattern: "allocat"
---

<objective>
Build the checkout wizard UI (3-step: address -> payment -> review), payment service (Stripe/credit/BACS), Stripe webhook, order success page with batch allocations and BACS bank details, and admin credit account management.

Purpose: This is the core e-commerce transaction UI. Plan 03 established the backend routers; this plan builds the frontend checkout experience and payment processing layer on top.
Output: Checkout wizard, payment service, Stripe webhook, success page, credit account admin.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-e-commerce-order-flow-wms/02-RESEARCH.md
@.planning/phases/02-e-commerce-order-flow-wms/02-01-SUMMARY.md
@.planning/phases/02-e-commerce-order-flow-wms/02-03-SUMMARY.md

@src/server/db/schema/orders.ts
@src/server/db/schema/addresses.ts
@src/server/db/schema/credit-accounts.ts
@src/server/db/schema/products.ts
@src/server/services/order-service.ts
@src/server/services/batch-service.ts
@src/stores/cart.ts
@src/server/trpc/trpc.ts
@src/server/trpc/routers/orders.ts
@src/server/trpc/routers/addresses.ts

<interfaces>
From Plan 03:
```typescript
// src/server/trpc/routers/addresses.ts
// addresses.list, addresses.create, addresses.update, addresses.delete, addresses.setDefault

// src/server/trpc/routers/orders.ts
// orders.create (validates prices server-side from products.pricePence)
// orders.getById, orders.confirmPayment, orders.getCreditAccount
```

From Plan 01:
```typescript
// src/server/services/order-service.ts
export function canTransition(from, to): boolean;
export function transitionOrder(tx, orderId, newStatus, metadata?): Promise<void>;
export function createOrder(tx, data): Promise<Order>;
export function allocateOrderStock(tx, orderId): Promise<FIFOAllocation[][]>;

// src/stores/cart.ts
export const useCartStore: CartStore; // items, addItem, removeItem, updateQuantity, clear

// src/server/db/schema/products.ts
// products table now has pricePence column
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Checkout wizard UI (3-step flow)</name>
  <files>
    src/app/(shop)/checkout/page.tsx,
    src/components/shop/CheckoutWizard.tsx,
    src/components/shop/CheckoutStep1Address.tsx,
    src/components/shop/CheckoutStep2Payment.tsx,
    src/components/shop/CheckoutStep3Review.tsx,
    src/__tests__/checkout/checkout-flow.test.ts
  </files>
  <action>
    **src/app/(shop)/checkout/page.tsx:** Protected page.
    - Redirects to /basket if cart is empty
    - Renders CheckoutWizard component

    **src/components/shop/CheckoutWizard.tsx:** "use client"
    - 3-step wizard with step indicator (reuse pattern from GoodsInForm)
    - State: currentStep (1-3), selectedAddressId, paymentMethod, poNumber
    - Step navigation: Back/Next buttons, validation before advancing
    - Final "Place Order" button on Step 3

    **CheckoutStep1Address.tsx:** Select delivery address from saved addresses or add new inline. Radio selection UI. Shows address details + site contact on each option. Uses addresses.list query from Plan 03.

    **CheckoutStep2Payment.tsx:**
    - PO Number input (required if payment method is 'credit', optional otherwise per user decision)
    - Payment method selector: Card (Stripe), Credit Terms, BACS Bank Transfer
    - Credit option only shown if user has approved credit account (check via orders.getCreditAccount)
    - If credit selected, show current credit limit and available balance
    - If BACS selected, show info text: "You'll receive a proforma invoice with bank details"

    **CheckoutStep3Review.tsx:**
    - Order summary: line items (product, qty, unit price, line total), subtotal, VAT (20%), total
    - Selected delivery address summary
    - Payment method and PO number summary
    - "Place Order" button triggers orders.create mutation
    - On success: if card -> redirect to Stripe Checkout URL (from processPayment); if credit/BACS -> redirect to /checkout/success?order={orderRef}

    **src/__tests__/checkout/checkout-flow.test.ts:** Fill in test stubs from Wave 0.
    - Test PO number is required for credit payment
    - Test PO number is optional for card/BACS
    - Test checkout rejects empty cart
    - Test order creation calls orders.create with correct payload
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/checkout --reporter=verbose 2>&1 | tail -15 && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>3-step checkout wizard renders all steps. Address selection from saved addresses works. Payment method selection with conditional credit option. Review step shows order summary with VAT. Place Order triggers order creation. Checkout tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Payment service, Stripe webhook, success page, and credit account admin</name>
  <files>
    src/server/services/payment-service.ts,
    src/app/api/stripe/webhook/route.ts,
    src/app/(shop)/checkout/success/page.tsx,
    src/app/(wms)/credit-accounts/page.tsx,
    src/components/wms/CreditAccountManager.tsx,
    src/__tests__/stripe/stripe-webhook.test.ts,
    src/__tests__/bacs/bacs-payment.test.ts
  </files>
  <action>
    **src/server/services/payment-service.ts:**
    - `createStripeCheckoutSession(orderId, orderRef, lineItems, customerEmail)`: Creates Stripe Checkout Session per research code example. mode: 'payment', currency: 'gbp', unit_amount in pence. metadata includes orderId and orderRef. success_url: /checkout/success?order={orderRef}, cancel_url: /basket. Returns session URL for redirect.
    - `validateCreditAccount(userId, totalPence)`: Checks user has approved credit account with sufficient available credit (creditLimitPence - creditUsedPence >= totalPence). Returns { valid, account, availableCredit } or throws.
    - `processPayment(tx, orderId, method, userId)`: Strategy pattern per research. Card: create Stripe session, transition to awaiting_payment, return sessionUrl. Credit: validate credit, deduct from available credit, transition to confirmed. BACS: transition to awaiting_payment.
    - Install stripe: `npm install stripe @stripe/stripe-js`
    - Use env: STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET

    **src/app/api/stripe/webhook/route.ts:**
    - POST handler per research code example
    - Verify signature with stripe.webhooks.constructEvent(body, sig, STRIPE_WEBHOOK_SECRET)
    - Handle 'checkout.session.completed' event: extract orderId from metadata, transition order awaiting_payment -> confirmed, run allocateOrderStock, store stripePaymentIntentId on order
    - Idempotency: check order status before transition -- if already confirmed, skip
    - Return 200 on success, 400 on invalid signature
    - CRITICAL: Use req.text() for raw body (not req.json()). App Router does not need bodyParser config.

    **src/app/(shop)/checkout/success/page.tsx:**
    - Takes ?order={orderRef} query param
    - Fetches order details via orders.getById (including allocations)
    - Shows: order reference, status, payment method, delivery address
    - Shows line items with batch allocation per line (TRACE-07): Product | Qty | Torke Batch ID(s) | Supplier Batch(es)
    - If status is 'awaiting_payment' (BACS): show bank details for payment (sort code, account number, reference = order number). Show message "Your order will be processed once payment is received." This serves as the BACS proforma per user decision -- the success page IS the proforma with bank details inline. A formal PDF proforma is generated by the invoice service in Plan 05.
    - If status is 'confirmed' or 'allocated': show "Order confirmed" with green checkmark
    - Link to "View Order History" (/account/orders)

    **src/app/(wms)/credit-accounts/page.tsx + CreditAccountManager.tsx:**
    - Admin page under WMS route group (warehouseProcedure protection)
    - Lists all credit account applications with status badges (pending, approved, rejected, suspended)
    - For pending accounts: Approve/Reject buttons
    - Approve dialog: set credit limit (GBP), select terms (Net 30 / Net 60)
    - Table columns: Company Name, User Email, Status, Credit Limit, Credit Used, Available, Terms, Actions
    - Add tRPC mutations to orders router: `orders.listCreditAccounts` (warehouse), `orders.approveCreditAccount` (warehouse), `orders.rejectCreditAccount` (warehouse)
    - Add WMS sidebar navigation link to "Credit Accounts" and "Orders" (for Plan 04)

    **src/__tests__/stripe/stripe-webhook.test.ts:** Fill in test stubs from Wave 0.
    - Mock stripe.webhooks.constructEvent
    - Test valid webhook transitions order to confirmed
    - Test idempotency: already-confirmed order is skipped
    - Test invalid signature returns 400

    **src/__tests__/bacs/bacs-payment.test.ts:** Fill in test stubs from Wave 0.
    - Test BACS payment transitions order to awaiting_payment
    - Test admin confirmPayment transitions to confirmed
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/stripe src/__tests__/bacs --reporter=verbose 2>&1 | tail -15 && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Payment service handles all 3 methods. Stripe webhook confirms payments and triggers allocation. Success page shows batch allocations (TRACE-07) and BACS bank details. Credit account admin works for approve/reject. Stripe and BACS tests pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npx vitest run src/__tests__/checkout src/__tests__/stripe src/__tests__/bacs --reporter=verbose` passes
- Checkout wizard steps navigate correctly
- Payment service has Stripe, credit, and BACS handlers
- Stripe webhook verifies signatures and handles checkout.session.completed
- Order success page displays batch allocations per line item
- BACS success page shows bank details inline (proforma function)
- Credit account admin can approve/reject with limits
</verification>

<success_criteria>
1. Credit checkout validates approved account and available limit (SHOP-08)
2. BACS checkout holds order at awaiting_payment, success page shows bank details (SHOP-09)
3. Stripe checkout redirects to Stripe-hosted page with SCA (SHOP-10)
4. Order confirmation shows batch allocation per line item (TRACE-07)
5. Admin can manage credit accounts (approve/reject/set limits)
6. Checkout, Stripe, and BACS tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-e-commerce-order-flow-wms/02-07-SUMMARY.md`
</output>
