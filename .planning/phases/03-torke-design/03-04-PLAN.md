---
phase: 03-torke-design
plan: 04
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/components/design/ResultsPanel.tsx
  - src/components/design/FailureModeBar.tsx
  - src/components/design/FailureModeDetail.tsx
  - src/components/design/OverallStatus.tsx
  - src/components/design/ActionBar.tsx
  - src/app/(design)/design/page.tsx
autonomous: true
requirements: [DESIGN-04]

must_haves:
  truths:
    - "User sees horizontal utilisation bars (0-100%) for each failure mode"
    - "Bars colour-transition: green (<60%), amber (60-90%), red (>90%)"
    - "Clicking a bar expands to show full calculation breakdown with clause references"
    - "Overall pass/fail status and governing failure mode shown at bottom"
    - "Sticky action bar with Export PDF and Save Calculation buttons always visible"
  artifacts:
    - path: "src/components/design/ResultsPanel.tsx"
      provides: "Right-panel results section with all failure mode bars"
      min_lines: 50
    - path: "src/components/design/FailureModeBar.tsx"
      provides: "Horizontal utilisation bar with expand/collapse"
    - path: "src/components/design/OverallStatus.tsx"
      provides: "Overall pass/fail and governing mode display"
    - path: "src/components/design/ActionBar.tsx"
      provides: "Sticky Export PDF and Save Calculation buttons"
  key_links:
    - from: "src/components/design/ResultsPanel.tsx"
      to: "src/stores/design.ts"
      via: "reads results from design store"
      pattern: "useDesignStore.*results"
    - from: "src/components/design/FailureModeBar.tsx"
      to: "src/lib/calc-engine/types.ts"
      via: "renders FailureModeResult data"
      pattern: "FailureModeResult"
---

<objective>
Build the results display panel showing utilisation ratios, expandable calculation breakdowns, overall pass/fail status, and the sticky action bar.

Purpose: Engineers need to see at a glance which failure modes pass/fail and drill into detailed calculations with EN 1992-4 clause references. The action bar provides always-visible access to Export PDF and Save Calculation.

Output: Complete results panel with interactive utilisation bars, detail expansions, and action bar.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-torke-design/03-RESEARCH.md
@.planning/phases/03-torke-design/03-01-SUMMARY.md
@.planning/phases/03-torke-design/03-02-SUMMARY.md

<interfaces>
From src/lib/calc-engine/types.ts (created in Plan 01):
```typescript
export interface FailureModeResult {
  name: string;
  clauseRef: string;           // e.g. "EN 1992-4 Cl. 7.2.1.4"
  designLoad: number;          // kN
  designResistance: number;    // kN
  utilisation: number;         // 0-1
  pass: boolean;
  intermediateValues: Record<string, { label: string; value: number; unit: string }>;
  formula: string;
}

export interface DesignResults {
  failureModes: FailureModeResult[];
  combinedInteraction: FailureModeResult;
  overallPass: boolean;
  governingMode: string;
  governingUtilisation: number;
  scopeLimitations: string[];
}
```

From src/stores/design.ts (created in Plan 02):
```typescript
export const useDesignStore: results: DesignResults | null
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Utilisation bars and expandable detail panels</name>
  <files>
    src/components/design/FailureModeBar.tsx,
    src/components/design/FailureModeDetail.tsx,
    src/components/design/ResultsPanel.tsx
  </files>
  <action>
    1. Create FailureModeBar.tsx:
       - Props: result (FailureModeResult), expanded (boolean), onToggle ()
       - Horizontal bar showing: failure mode name (left), utilisation percentage (right), pass/fail icon
       - Bar fill width = utilisation * 100%
       - Colour transitions: green (#22C55E) for utilisation < 0.6, amber (#EAB308) for 0.6-0.9, red (#EF4444) for > 0.9
       - Background: dark grey bar (#1A1A1A) with coloured fill
       - Clickable: toggles expanded state to show FailureModeDetail
       - Chevron icon (right side) rotates on expand
       - Pass: green checkmark icon. Fail: red X icon
       - Show percentage as "42%" format

    2. Create FailureModeDetail.tsx:
       - Props: result (FailureModeResult)
       - Expandable panel below the bar (shadcn Collapsible or height transition)
       - Shows: clause reference (e.g. "EN 1992-4 Cl. 7.2.1.4") as a badge/tag
       - Design load vs Design resistance: "NEd = 15.0 kN" vs "NRd,c = 35.2 kN"
       - Formula: display the formula string (plain text, monospace font)
       - Intermediate values table: two-column layout showing each intermediate value with label, value, and unit (e.g. "k1 = 7.7 (cracked concrete)", "Ac,N = 67500 mm2")
       - Dark card background (#111) with subtle border

    3. Create ResultsPanel.tsx:
       - Reads results from useDesignStore
       - If results is null: show skeleton/placeholder "Adjust parameters to see results"
       - If results exists: render list of FailureModeBar for each failureMode + combinedInteraction
       - Manage expanded state locally (useState for which bar index is expanded, allow multiple)
       - Section header: "Failure Mode Checks" with count badge "8 checks"
       - Scrollable if content overflows
       - Scope limitations: show as a subtle warning banner at top of results (amber border, "Limitations" heading, bullet list of scopeLimitations)
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -10</automated>
  </verify>
  <done>ResultsPanel renders utilisation bars for all failure modes. Bars show correct colours. Clicking expands to show clause refs, formula, intermediate values. Scope limitations displayed.</done>
</task>

<task type="auto">
  <name>Task 2: Overall status, action bar, and page wiring</name>
  <files>
    src/components/design/OverallStatus.tsx,
    src/components/design/ActionBar.tsx,
    src/app/(design)/design/page.tsx
  </files>
  <action>
    1. Create OverallStatus.tsx:
       - Reads results from useDesignStore
       - Large pass/fail indicator: green "PASS" with checkmark or red "FAIL" with X
       - Governing failure mode: "Governed by: Concrete Cone Breakout (87%)"
       - If pass: green gradient background with subtle glow
       - If fail: red gradient background
       - Compact layout: one line for status, one for governing mode

    2. Create ActionBar.tsx:
       - Sticky bar at bottom of results panel (sticky bottom-0)
       - Two buttons: "Export PDF" (primary, red #C41E3A) and "Save Calculation" (secondary, outline)
       - Both buttons disabled when results is null
       - Both buttons show lock icon when user is not authenticated (visual hint)
       - onClick handlers are stubs for now: they will check auth and either proceed or show AuthGateModal (implemented in Plan 06)
       - Export PDF button: onClick stub sets a flag or calls a placeholder function
       - Save Calculation button: same stub pattern
       - Include DESIGN-07 scope disclaimer text below buttons: small text "Calculations exclude seismic and fire design"

    3. Wire into design page (src/app/(design)/design/page.tsx):
       - Add ResultsPanel below the 3D scene in the right panel
       - Add OverallStatus between 3D scene and ResultsPanel
       - Add ActionBar at bottom of right panel (sticky)
       - Ensure the debounced recalculation effect (from Plan 03) stores results in the design store
       - If Plan 03 wiring isn't done yet, add the debounced useEffect here:
         ```
         useEffect with deps on inputs -> debounce 300ms -> calculateAnchorDesign(inputs) -> setResults
         ```
       - Right panel layout: 3D scene (50-60% height) | OverallStatus (compact) | ResultsPanel (scrollable, flex-1) | ActionBar (sticky bottom)
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -10</automated>
  </verify>
  <done>Overall pass/fail status renders with governing mode. Action bar is sticky with Export PDF and Save buttons. Full design page shows inputs, 3D model, results, and action bar. Changing inputs triggers recalculation and updates results in real time.</done>
</task>

</tasks>

<verification>
- /design shows utilisation bars after changing any input
- Bars show green/amber/red based on utilisation value
- Clicking a bar expands to show clause reference, formula, and intermediate values
- Overall pass/fail indicator shows at bottom with governing failure mode
- Action bar is sticky and always visible
- Scope limitations banner visible in results
</verification>

<success_criteria>
- Utilisation ratio bars for all failure modes (DESIGN-04)
- Expandable detail with EN 1992-4 clause references
- Overall pass/fail with governing mode identification
- Sticky Export PDF and Save Calculation buttons
- Scope limitations clearly stated (DESIGN-07 partial)
- Live updates when inputs change
</success_criteria>

<output>
After completion, create `.planning/phases/03-torke-design/03-04-SUMMARY.md`
</output>
