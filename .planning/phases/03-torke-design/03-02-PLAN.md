---
phase: 03-torke-design
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/db/schema/calculations.ts
  - src/server/db/schema/orders.ts
  - src/server/db/schema/index.ts
  - src/stores/cart.ts
  - src/stores/design.ts
  - src/app/(design)/design/page.tsx
  - src/app/(design)/layout.tsx
  - src/components/design/InputPanel.tsx
  - src/components/design/sections/ProjectInfoSection.tsx
  - src/components/design/sections/AnchorTypeSection.tsx
  - src/components/design/sections/ConcreteSection.tsx
  - src/components/design/sections/LoadsSection.tsx
  - src/components/design/sections/GeometrySection.tsx
  - src/components/design/sections/EnvironmentSection.tsx
autonomous: true
requirements: [DESIGN-02, DESIGN-17]

must_haves:
  truths:
    - "User can see the design page at /design without logging in"
    - "User can enter all EN 1992-4 input parameters via grouped collapsible sections"
    - "Input changes update the Zustand design store which persists in localStorage"
    - "CartItem interface supports optional calcReference field"
    - "calculations table exists in DB schema for saved calculations"
    - "orderLines has calcReference column linking orders to calculations"
  artifacts:
    - path: "src/stores/design.ts"
      provides: "Zustand design store with inputs, results, setInput, loadPreset"
      exports: ["useDesignStore"]
    - path: "src/server/db/schema/calculations.ts"
      provides: "Saved calculations table"
      exports: ["calculations"]
    - path: "src/components/design/InputPanel.tsx"
      provides: "Left panel with 6 collapsible input sections"
    - path: "src/app/(design)/design/page.tsx"
      provides: "Design tool page at /design"
  key_links:
    - from: "src/stores/design.ts"
      to: "src/lib/calc-engine/types.ts"
      via: "imports DesignInputs type"
      pattern: "import.*DesignInputs.*calc-engine"
    - from: "src/components/design/InputPanel.tsx"
      to: "src/stores/design.ts"
      via: "reads/writes design store"
      pattern: "useDesignStore"
    - from: "src/stores/cart.ts"
      to: "CartItem.calcReference"
      via: "optional field on CartItem"
      pattern: "calcReference"
---

<objective>
Set up the database schema extensions, design store, and input panel UI for Torke TRACE. This creates the data layer and input surface that all other plans build on.

Purpose: Establish the foundation for the design tool: DB tables for saved calcs, cart/order schema extensions for calc references, the Zustand store that bridges inputs to the calc engine, and the full input form UI.

Output: Working /design page with input panel, design Zustand store, and schema extensions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-torke-design/03-RESEARCH.md

<interfaces>
From src/stores/cart.ts:
```typescript
export interface CartItem {
  productId: string;
  productName: string;
  sku: string;
  quantity: number;
  unitPricePence: number;
}
```

From src/server/db/schema/orders.ts:
```typescript
export const orderLines = pgTable("order_lines", {
  id: uuid("id").primaryKey().defaultRandom(),
  orderId: uuid("order_id").references(() => orders.id, { onDelete: "cascade" }).notNull(),
  productId: uuid("product_id").references(() => products.id).notNull(),
  quantity: integer("quantity").notNull(),
  unitPricePence: integer("unit_price_pence").notNull(),
  lineTotalPence: integer("line_total_pence").notNull(),
  createdAt: timestamp("created_at").defaultNow().notNull(),
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema extensions and store setup</name>
  <files>
    src/server/db/schema/calculations.ts,
    src/server/db/schema/orders.ts,
    src/server/db/schema/index.ts,
    src/stores/cart.ts,
    src/stores/design.ts
  </files>
  <action>
    1. Create src/server/db/schema/calculations.ts with:
       - calculations table: id (uuid PK), calcReference (varchar 50, unique, not null), userId (text, not null), projectName (varchar 255, nullable), engineerName (varchar 255, nullable), inputs (jsonb, not null), results (jsonb, not null), createdAt, updatedAt
       - Export table and add to schema index

    2. Add calcReference column to orderLines in src/server/db/schema/orders.ts:
       - calcReference: varchar("calc_reference", { length: 50 }) -- nullable, optional link to calculation

    3. Re-export calculations from src/server/db/schema/index.ts

    4. Extend CartItem interface in src/stores/cart.ts:
       - Add optional `calcReference?: string` field
       - Update addItem to pass through calcReference when present
       - When an item with the same productId is added but with a different calcReference, treat as a separate line item (don't merge quantities)

    5. Create src/stores/design.ts Zustand store with persist middleware:
       - State: inputs (DesignInputs), results (DesignResults | null), isCalculating (boolean), calcReference (string | null for saved calcs)
       - Actions: setInput(key, value), setInputs(partial), loadPreset(preset), setResults(results), setCalculating(boolean), reset()
       - Import DesignInputs and DesignResults types from @/lib/calc-engine/types (these types will be created by Plan 01 -- if not yet available, create local placeholder types that match the spec from RESEARCH.md and add a TODO comment to align once Plan 01 completes)
       - DEFAULT_INPUTS: use the "Single M12 baseplate" preset values as defaults
       - Persist key: "torke-design"
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>calculations table defined in schema. orderLines has calcReference column. CartItem has optional calcReference. useDesignStore exports working Zustand store with persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Input panel UI with collapsible sections</name>
  <files>
    src/app/(design)/layout.tsx,
    src/app/(design)/design/page.tsx,
    src/components/design/InputPanel.tsx,
    src/components/design/sections/ProjectInfoSection.tsx,
    src/components/design/sections/AnchorTypeSection.tsx,
    src/components/design/sections/ConcreteSection.tsx,
    src/components/design/sections/LoadsSection.tsx,
    src/components/design/sections/GeometrySection.tsx,
    src/components/design/sections/EnvironmentSection.tsx
  </files>
  <action>
    1. Create (design) route group layout at src/app/(design)/layout.tsx:
       - Import existing Header component for site navigation
       - Full-height layout (h-screen flex flex-col)
       - Body area fills remaining space (flex-1 overflow-hidden)
       - Dark theme matching Torke brand (#0A0A0A background)
       - No Footer (design tool needs maximum vertical space)

    2. Create design page at src/app/(design)/design/page.tsx:
       - Split-panel layout: left panel (inputs, scrollable, w-[400px] on desktop) | right panel (3D + results, flex-1)
       - Left panel renders InputPanel component
       - Right panel is placeholder for now (grey box with "3D Visualisation" and "Results" placeholder text) -- filled by Plans 03 and 04
       - No auth guard (DESIGN-17: calculations work without login)
       - Mobile: stack vertically (inputs on top, results below)

    3. Create InputPanel component:
       - Scrollable container with all 6 sections
       - Each section is a collapsible card (shadcn Collapsible or custom) -- all expanded by default
       - Preset selector at top: dropdown with presets from calc engine (or hardcoded list if Plan 01 not complete yet)
       - Reads from and writes to useDesignStore

    4. Create 6 input section components, each reading/writing to useDesignStore:

       ProjectInfoSection: projectName (text), engineerName (text), projectRef (text), date (auto-filled, readonly)

       AnchorTypeSection: anchorType select (chemical/mechanical), anchorDiameter select (M8/M10/M12/M16/M20/M24/M30), steelGrade select (5.8/8.8/10.9/A4-70/A4-80), embedmentDepth number input (mm, min 40, max 500)

       ConcreteSection: concreteClass select (C20/25 through C50/60), crackedConcrete toggle (boolean), memberThickness number input (mm, min 100)

       LoadsSection: tensionLoad number input (kN, NEd, min 0), shearLoad number input (kN, VEd, min 0), load combination info text

       GeometrySection: groupPattern select (single/2x1/2x2/3x2), spacing s1 and s2 inputs (mm, shown when group), edgeDistances c1/c2/c3/c4 inputs (mm), plateThickness/plateWidth/plateDepth inputs (mm)

       EnvironmentSection: environment select (dry/humid/marine), corrosion info text based on selection

    All inputs use shadcn form components. Number inputs have unit labels (mm, kN). Validation inline with red borders and error messages.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>User can visit /design and see split-panel layout with all 6 input sections. Inputs write to design store. Presets load default values. Right panel has placeholder content. No login required.</done>
</task>

</tasks>

<verification>
- Visit /design -- page loads without auth
- All 6 input sections render with correct field types
- Changing inputs updates the Zustand store (check localStorage "torke-design" key)
- Preset selector loads different default values
- Mobile layout stacks panels vertically
</verification>

<success_criteria>
- /design route accessible without login (DESIGN-17)
- All DESIGN-02 input fields present and functional
- Design store persists across page refresh
- Schema extensions ready for saved calcs and calc-to-order linking
- Cart supports optional calcReference on items
</success_criteria>

<output>
After completion, create `.planning/phases/03-torke-design/03-02-SUMMARY.md`
</output>
