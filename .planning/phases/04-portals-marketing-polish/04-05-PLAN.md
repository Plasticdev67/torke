---
phase: 04-portals-marketing-polish
plan: 05
type: execute
wave: 3
depends_on: ["04-01", "04-03"]
files_modified:
  - src/server/db/schema/users.ts
  - src/server/trpc/routers/leads.ts
  - src/server/trpc/router.ts
  - src/app/(wms)/warehouse/admin/leads/page.tsx
  - src/components/design/SoftPrompt.tsx
  - src/app/(shop)/design/page.tsx
  - src/app/(shop)/account/page.tsx
  - src/components/shop/AccountDashboard.tsx
autonomous: true
requirements:
  - MKTG-05
  - MKTG-06
  - MKTG-07

must_haves:
  truths:
    - "Torke Design is accessible without login for calculations"
    - "Saving or exporting a calculation requires free account with email, name, and company captured"
    - "Account created via Torke Design works in e-commerce shop (single account system)"
    - "After multiple calculations, user sees a soft non-blocking prompt to save their work"
    - "Admin can view a list of recent signups from the design tool"
    - "Account dashboard surfaces both calculations and orders for unified users"
  artifacts:
    - path: "src/components/design/SoftPrompt.tsx"
      provides: "Non-blocking account prompt for design tool users"
      min_lines: 20
    - path: "src/app/(wms)/warehouse/admin/leads/page.tsx"
      provides: "Admin lead list page"
      min_lines: 30
    - path: "src/server/trpc/routers/leads.ts"
      provides: "Lead list tRPC router"
      exports: ["leadsRouter"]
  key_links:
    - from: "src/components/design/SoftPrompt.tsx"
      to: "src/app/(shop)/design/page.tsx"
      via: "Rendered conditionally after N calculations"
      pattern: "SoftPrompt"
    - from: "src/app/(wms)/warehouse/admin/leads/page.tsx"
      to: "src/server/trpc/routers/leads.ts"
      via: "tRPC leads.list query"
      pattern: "trpc\\.leads\\.list"
---

<objective>
Polish the Torke Design lead generation funnel: verify free access works, ensure company field is captured at registration, add soft non-blocking prompts after multiple calculations, create a welcome flow for design-to-shop transition, build the admin lead list, and surface calculations in the unified account dashboard.

Purpose: Activates Torke Design as a lead generation funnel -- engineers find the tool via search, use it freely, are captured when they save/export, and are smoothly transitioned to the e-commerce experience.
Output: SoftPrompt component, admin lead list page, enhanced registration flow, unified account dashboard.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-portals-marketing-polish/04-01-SUMMARY.md
@.planning/phases/04-portals-marketing-polish/04-03-SUMMARY.md
@src/app/(shop)/design/page.tsx
@src/components/shop/AccountDashboard.tsx
@src/server/db/schema/users.ts
@src/server/db/schema/calculations.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lead capture polish -- company field, soft prompts, design-to-shop welcome</name>
  <files>src/server/db/schema/users.ts, src/components/design/SoftPrompt.tsx, src/app/(shop)/design/page.tsx, src/app/(shop)/account/page.tsx, src/components/shop/AccountDashboard.tsx</files>
  <action>
1. Check `src/server/db/schema/users.ts` for a company/companyName field on the user profile. If missing, add `companyName: varchar("company_name", { length: 255 })` to the user table or profile table. Check the Better Auth registration flow and ensure the registration form captures company name alongside email and name. If the register page (`src/app/(auth)/register/page.tsx` or similar) doesn't have a company field, add it. Store in the user profile.

2. Create `src/components/design/SoftPrompt.tsx` (client component):
   - Non-blocking banner/toast that appears after the user has performed 3+ calculations without being logged in.
   - Track calculation count in localStorage (key: "torke_calc_count").
   - Message: "You've run [N] calculations. Create a free account to save your work and export PDF reports."
   - CTA button: "Create Free Account" linking to registration.
   - Dismissible (X button). Once dismissed, don't show again for 24 hours (store dismiss timestamp in localStorage).
   - Positioned as a bottom banner or slide-in from bottom-right. Does NOT block interaction with the design tool.
   - Torke red accent on CTA button.

3. Wire SoftPrompt into the design page (`src/app/(shop)/design/page.tsx` or the design tool layout):
   - Render SoftPrompt only when user is not authenticated.
   - Increment calc count in localStorage each time a calculation runs (hook into existing debounced recalculation).

4. Design-to-shop welcome: In `src/components/shop/AccountDashboard.tsx`, add a "Torke Design" section:
   - If user has saved calculations, show count and link to /account/calculations (if that page exists from Phase 3) or /design.
   - If user has calculations but no orders, show a gentle prompt: "Ready to order? The fixings you designed are available in our shop." with link to /products.
   - This creates the unified experience (MKTG-07) where design and shop are visible in one dashboard.

5. Verify MKTG-05: Confirm /design is accessible in incognito without login. The design tool should work fully for calculations. Only save/export should be gated. This was implemented in Phase 3 -- just verify no regressions by checking the auth gate logic.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Company field captured at registration. Soft prompt appears after 3 calculations for anonymous users. Account dashboard shows unified view of calculations and orders. Design tool accessible without login.</done>
</task>

<task type="auto">
  <name>Task 2: Admin lead list page</name>
  <files>src/server/trpc/routers/leads.ts, src/server/trpc/router.ts, src/app/(wms)/warehouse/admin/leads/page.tsx</files>
  <action>
1. Create `src/server/trpc/routers/leads.ts`:
   - `list` warehouseProcedure (admin-only): Query users who have saved calculations. Return: name, email, companyName, createdAt, calculationCount (count of saved calculations per user). Sort by createdAt DESC. Paginated (limit/offset).
   - `stats` warehouseProcedure: Return summary: total design signups, signups this week, signups this month, users who also have orders (converted leads).
   - Join users with calculations table to get calculation count.
   - Filter: optionally by date range, has_orders (boolean to show converted vs unconverted).

2. Register leadsRouter in main router.

3. Create `src/app/(wms)/warehouse/admin/leads/page.tsx`:
   - Admin page in WMS layout (dark sidebar, requires warehouse role).
   - Summary cards at top: Total Leads, This Week, This Month, Converted (have orders).
   - Table: Name, Email, Company, Signup Date, Calculations, Has Orders (badge).
   - Pagination controls.
   - Simple date filter and converted/unconverted toggle.
   - No email automation (deferred to v2) -- this is a read-only list for validating the funnel.

4. Add "Leads" link to the WMS admin navigation sidebar (check existing sidebar component and add alongside existing WMS nav items like Stock, Orders, Batches).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Admin lead list page shows recent design tool signups with name, email, company, date, calculation count, and conversion status. Summary stats at top. Accessible via WMS admin sidebar.</done>
</task>

</tasks>

<verification>
- Visit /design in incognito -- design tool works without login (MKTG-05)
- Run 3+ calculations without login -- soft prompt appears
- Dismiss prompt -- doesn't reappear immediately
- Register new account -- company name field present and saved (MKTG-06)
- Log in and visit /account -- dashboard shows calculations section and shop prompt (MKTG-07)
- Visit /warehouse/admin/leads as admin -- lead list with signups, stats cards, pagination
</verification>

<success_criteria>
- Torke Design accessible without login (MKTG-05 verified)
- Registration captures email, name, company; account creation from Design feeds into e-commerce system (MKTG-06, MKTG-07)
- Soft non-blocking prompts appear after multiple anonymous calculations
- Account dashboard unifies design calculations and shop orders
- Admin lead list validates the funnel with basic metrics
</success_criteria>

<output>
After completion, create `.planning/phases/04-portals-marketing-polish/04-05-SUMMARY.md`
</output>
