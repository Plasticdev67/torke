---
phase: 04-portals-marketing-polish
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/server/trpc/routers/verification.ts
  - src/server/trpc/router.ts
  - src/app/v/[token]/page.tsx
  - src/app/t/[token]/page.tsx
  - src/components/shop/ShareLinkDialog.tsx
  - src/app/(shop)/account/certifications/page.tsx
autonomous: true
requirements:
  - TRACE-15
  - TRACE-16
  - TRACE-17

must_haves:
  truths:
    - "Contractor can generate a unique read-only verification link for an order"
    - "End-client can view project name, fixings, batches, and certs via the verification link without logging in"
    - "Verification page shows co-branded display with contractor company name and Torke branding"
    - "QR code on physical product label resolves to public verification page showing batch details and 3.1 cert"
    - "Verification links never expire"
  artifacts:
    - path: "src/server/trpc/routers/verification.ts"
      provides: "Share token generation and management"
      exports: ["verificationRouter"]
    - path: "src/app/v/[token]/page.tsx"
      provides: "Order-level verification page for end-clients"
      min_lines: 60
    - path: "src/components/shop/ShareLinkDialog.tsx"
      provides: "Dialog for generating share links"
      min_lines: 30
  key_links:
    - from: "src/components/shop/ShareLinkDialog.tsx"
      to: "src/server/trpc/routers/verification.ts"
      via: "tRPC verification.createShareLink mutation"
      pattern: "trpc\\.verification\\.createShareLink"
    - from: "src/app/v/[token]/page.tsx"
      to: "src/server/db/schema/share-tokens.ts"
      via: "Direct DB query for share token + order data"
      pattern: "orderShareTokens"
---

<objective>
Build the end-client verification portal: contractors can generate shareable read-only verification links for orders, end-clients can view full traceability data (project name, fixings, batches, 3.1 certs) without logging in. Also verify existing QR-to-cert verification flow (TRACE-17).

Purpose: Enables contractors to share verifiable supply chain documentation with building owners, structural engineers, and building control -- closing the traceability loop to end-clients.
Output: Verification tRPC router, ShareLinkDialog component, /v/[token] public verification page.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-portals-marketing-polish/04-01-SUMMARY.md
@src/app/t/[token]/page.tsx
@src/server/db/schema/share-tokens.ts
@src/server/db/schema/verification.ts
@src/server/services/qr-service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verification router, share link dialog, and order verification page</name>
  <files>src/server/trpc/routers/verification.ts, src/server/trpc/router.ts, src/components/shop/ShareLinkDialog.tsx, src/app/(shop)/account/certifications/page.tsx</files>
  <action>
1. Create `src/server/trpc/routers/verification.ts`:
   - `createShareLink` protectedProcedure: input {orderId, projectName?}. Validates user owns the order. Creates orderShareTokens row. Returns full URL `/v/{token}` and the token. Also generates a QR code data URL for the share link via qr-service.
   - `getShareLinks` protectedProcedure: input {orderId}. Returns all share tokens for an order owned by the user. Allows contractors to see existing links.
   - `revokeShareLink` protectedProcedure: input {tokenId}. Deletes share token (user owns it). Even though links "never expire" per requirement, allow revocation for security.

2. Register verificationRouter in main router.

3. Create `src/components/shop/ShareLinkDialog.tsx` (client component):
   - Dialog/modal triggered from cert results (Plan 01 CertResults component).
   - Input field for optional project name (for co-branding on verification page).
   - "Generate Link" button calls verification.createShareLink.
   - Displays the generated URL with copy-to-clipboard button.
   - Shows QR code image (for printing/sharing).
   - Lists existing share links for this order with copy + revoke buttons.

4. Wire ShareLinkDialog into the certifications page / CertResults component:
   - Add a "Share" button per order in CertResults that opens ShareLinkDialog.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Contractor can generate a share link with optional project name. Link is copyable with QR code. Existing links visible and revocable.</done>
</task>

<task type="auto">
  <name>Task 2: Public order verification page and QR verification check</name>
  <files>src/app/v/[token]/page.tsx, src/app/t/[token]/page.tsx</files>
  <action>
1. Create `src/app/v/[token]/page.tsx`:
   - Server component. No auth required (public page).
   - Metadata: robots "noindex, nofollow", title "Order Verification | Torke".
   - Query orderShareTokens by token. If not found, render VerificationNotFound.
   - Update lastAccessedAt on the share token.
   - Fetch full order data: order lines with products, allocations with batches and supplier batch data.
   - Fetch creator's profile for contractor company name (co-branding).
   - LIGHT THEME (exception to dark site theme, matching existing /t/[token] pattern for certificate-like trustworthiness).
   - Layout:
     - Torke header with logo (red bar).
     - Co-branded banner: "Fixings supplied by [Contractor Company], verified by Torke".
     - Project name (if provided when share link was created).
     - Order reference and dispatch date.
     - Table of fixings: product name, SKU, quantity, Torke batch ID, supplier batch ID, supplier/manufacturer name, heat number, goods-in date, dispatch date.
     - Per-line "Download 3.1 Certificate" link (presigned R2 URL for individual batch cert).
     - Footer: "This is a verified supply chain record from Torke. Batch data is captured at goods-in and linked through dispatch."
   - Mobile-responsive (contractors often share these on site where inspectors view on phones).

2. Verify existing `/t/[token]` page (TRACE-17):
   - Read the existing page and confirm it shows: product details, batch ID, 3.1 cert, and goods-in date.
   - If missing any of these fields, add them. The existing page may already satisfy TRACE-17 -- check and augment if needed.
   - Ensure it uses light theme and noindex/nofollow (likely already done per STATE.md decisions).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>End-client can view full order traceability data via /v/[token] without logging in. Co-branded display with contractor company name. Individual cert downloads available. Existing /t/[token] QR verification page confirmed functional for TRACE-17.</done>
</task>

</tasks>

<verification>
- From certifications page, click "Share" on an order -- ShareLinkDialog opens
- Enter project name and click "Generate Link" -- URL and QR code displayed
- Copy the /v/[token] URL and open in incognito (no login) -- verification page loads
- Verification page shows light theme, co-branding, all fixings with batch details
- Click "Download 3.1 Certificate" on a line item -- PDF downloads
- Visit existing /t/[token] URL (from QR label) -- batch verification page loads with product details, batch ID, cert
</verification>

<success_criteria>
- Contractor can generate unique read-only verification link for end-client (TRACE-15)
- End-client sees project name, fixings, batches, and 3.1 certs without login (TRACE-16)
- QR code on physical label resolves to verification page with batch details and cert (TRACE-17)
- Verification pages are noindex/nofollow
- Co-branded with contractor company name and Torke identity
</success_criteria>

<output>
After completion, create `.planning/phases/04-portals-marketing-polish/04-02-SUMMARY.md`
</output>
