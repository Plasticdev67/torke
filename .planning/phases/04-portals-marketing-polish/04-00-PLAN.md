---
phase: 04-portals-marketing-polish
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/certs/cert-search.test.ts
  - src/__tests__/certs/bulk-download.test.ts
  - src/__tests__/certs/cert-links.test.ts
  - src/__tests__/verification/share-token.test.ts
  - src/__tests__/verification/order-verify.test.ts
  - src/__tests__/blog/mdx-utils.test.ts
  - src/__tests__/seo/schema-markup.test.ts
  - src/__tests__/blog/glossary.test.ts
  - src/__tests__/leads/lead-capture.test.ts
autonomous: true
requirements:
  - TRACE-12
  - TRACE-13
  - TRACE-14
  - TRACE-15
  - TRACE-16
  - MKTG-01
  - MKTG-02
  - MKTG-04
  - MKTG-06

must_haves:
  truths:
    - "Every Wave 1+ task has a pre-existing test stub to run against"
    - "vitest discovers and runs all 9 stub files with 0 failures (pending/skipped tests)"
  artifacts:
    - path: "src/__tests__/certs/cert-search.test.ts"
      provides: "Test stubs for TRACE-12 cert search"
    - path: "src/__tests__/certs/bulk-download.test.ts"
      provides: "Test stubs for TRACE-13 bulk download"
    - path: "src/__tests__/certs/cert-links.test.ts"
      provides: "Test stubs for TRACE-14 cert links"
    - path: "src/__tests__/verification/share-token.test.ts"
      provides: "Test stubs for TRACE-15 share tokens"
    - path: "src/__tests__/verification/order-verify.test.ts"
      provides: "Test stubs for TRACE-16 order verification"
    - path: "src/__tests__/blog/mdx-utils.test.ts"
      provides: "Test stubs for MKTG-01 MDX utilities"
    - path: "src/__tests__/seo/schema-markup.test.ts"
      provides: "Test stubs for MKTG-02 schema markup"
    - path: "src/__tests__/blog/glossary.test.ts"
      provides: "Test stubs for MKTG-04 glossary"
    - path: "src/__tests__/leads/lead-capture.test.ts"
      provides: "Test stubs for MKTG-06 lead capture"
  key_links: []
---

<objective>
Create test stub files for all Phase 4 requirements that have automated verification. These stubs ensure the Nyquist sampling rule is satisfied: every Wave 1+ task can reference a concrete test file in its verify step.

Purpose: Establishes the test scaffold before implementation begins, following the Phase 2 precedent (02-00-PLAN.md). Stubs contain describe blocks with `it.todo()` placeholders that implementers will flesh out during execution.
Output: 9 test stub files across 5 test directories, all discoverable by vitest.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-portals-marketing-polish/04-VALIDATION.md
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stub files for certs, verification, blog, SEO, and leads</name>
  <files>src/__tests__/certs/cert-search.test.ts, src/__tests__/certs/bulk-download.test.ts, src/__tests__/certs/cert-links.test.ts, src/__tests__/verification/share-token.test.ts, src/__tests__/verification/order-verify.test.ts, src/__tests__/blog/mdx-utils.test.ts, src/__tests__/seo/schema-markup.test.ts, src/__tests__/blog/glossary.test.ts, src/__tests__/leads/lead-capture.test.ts</files>
  <action>
Create 9 test stub files. Each file contains a `describe` block with `it.todo()` entries that map to the requirement's expected behaviors. Use vitest imports (`describe`, `it`, `expect`). No implementation -- just the skeleton.

1. `src/__tests__/certs/cert-search.test.ts` (TRACE-12):
   ```ts
   import { describe, it, expect } from "vitest";
   describe("Cert Search", () => {
     it.todo("searches by order number");
     it.todo("searches by Torke batch ID");
     it.todo("searches by product code");
     it.todo("searches by date range");
     it.todo("returns only orders belonging to authenticated user");
     it.todo("paginates results with limit and offset");
   });
   ```

2. `src/__tests__/certs/bulk-download.test.ts` (TRACE-13):
   ```ts
   import { describe, it, expect } from "vitest";
   describe("Bulk Cert Download", () => {
     it.todo("generates ZIP from multiple order cert packs");
     it.todo("enforces max 20 orders limit");
     it.todo("validates user owns all requested orders");
     it.todo("returns application/zip content type");
   });
   ```

3. `src/__tests__/certs/cert-links.test.ts` (TRACE-14):
   ```ts
   import { describe, it, expect } from "vitest";
   describe("Cert Links", () => {
     it.todo("returns individual 3.1 cert download URL per batch allocation");
     it.todo("returns cert pack download URL per order");
     it.todo("shows batch allocation details per order line");
   });
   ```

4. `src/__tests__/verification/share-token.test.ts` (TRACE-15):
   ```ts
   import { describe, it, expect } from "vitest";
   describe("Share Token", () => {
     it.todo("creates share token for an order owned by user");
     it.todo("rejects share token creation for unowned order");
     it.todo("returns full URL and QR code data URL");
     it.todo("lists existing share tokens for an order");
     it.todo("revokes a share token");
     it.todo("accepts optional project name");
   });
   ```

5. `src/__tests__/verification/order-verify.test.ts` (TRACE-16):
   ```ts
   import { describe, it, expect } from "vitest";
   describe("Order Verification Page", () => {
     it.todo("loads order data from valid share token");
     it.todo("returns not found for invalid token");
     it.todo("includes contractor company name for co-branding");
     it.todo("includes product details with batch IDs and quantities");
     it.todo("includes downloadable 3.1 cert links");
     it.todo("updates lastAccessedAt on token access");
   });
   ```

6. `src/__tests__/blog/mdx-utils.test.ts` (MKTG-01):
   ```ts
   import { describe, it, expect } from "vitest";
   describe("MDX Utilities", () => {
     it.todo("getAllPosts returns sorted array of posts");
     it.todo("getPostBySlug returns post with content");
     it.todo("parses frontmatter correctly");
     it.todo("calculates reading time estimate");
     it.todo("handles missing slug gracefully");
   });
   ```

7. `src/__tests__/seo/schema-markup.test.ts` (MKTG-02):
   ```ts
   import { describe, it, expect } from "vitest";
   describe("Schema Markup", () => {
     it.todo("productSchema returns valid Product JSON-LD");
     it.todo("articleSchema returns valid Article JSON-LD");
     it.todo("organizationSchema returns valid Organization JSON-LD");
     it.todo("breadcrumbSchema returns valid BreadcrumbList JSON-LD");
     it.todo("faqSchema returns valid FAQPage JSON-LD");
   });
   ```

8. `src/__tests__/blog/glossary.test.ts` (MKTG-04):
   ```ts
   import { describe, it, expect } from "vitest";
   describe("Glossary", () => {
     it.todo("glossary.json contains at least 25 terms");
     it.todo("each term has term, definition, and category fields");
     it.todo("categories are valid enum values");
     it.todo("GlossarySearch filters terms by search input");
     it.todo("GlossarySearch filters terms by category");
   });
   ```

9. `src/__tests__/leads/lead-capture.test.ts` (MKTG-06):
   ```ts
   import { describe, it, expect } from "vitest";
   describe("Lead Capture", () => {
     it.todo("leads.list returns users with saved calculations");
     it.todo("leads.list includes companyName and calculationCount");
     it.todo("leads.stats returns signup counts and conversion count");
     it.todo("leads.list is restricted to warehouse role");
     it.todo("leads.list supports pagination");
   });
   ```

Ensure all directories exist before writing files (create `src/__tests__/certs/`, `src/__tests__/verification/`, `src/__tests__/blog/`, `src/__tests__/seo/`, `src/__tests__/leads/`).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/certs/ src/__tests__/verification/ src/__tests__/blog/ src/__tests__/seo/ src/__tests__/leads/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All 9 test stub files exist and are discovered by vitest. All tests are in todo/skipped state with 0 failures. VALIDATION.md Wave 0 requirements satisfied.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` discovers all 9 new test files
- All tests are `todo` (no failures, no passes -- just pending stubs)
- Directory structure: src/__tests__/{certs,verification,blog,seo,leads}/ all exist
</verification>

<success_criteria>
- 9 test stub files created matching VALIDATION.md Wave 0 requirements
- vitest runs cleanly with all stubs in todo state
- Wave 1+ plans can reference specific test files in their verify commands
</success_criteria>

<output>
After completion, create `.planning/phases/04-portals-marketing-polish/04-00-SUMMARY.md`
</output>
