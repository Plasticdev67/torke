import { describe, it, expect } from "vitest";

describe("Share Token", () => {
  it.todo("creates share token for an order owned by user");
  it.todo("rejects share token creation for unowned order");
  it.todo("returns full URL and QR code data URL");
  it.todo("lists existing share tokens for an order");
  it.todo("revokes a share token");
  it.todo("accepts optional project name");
});
