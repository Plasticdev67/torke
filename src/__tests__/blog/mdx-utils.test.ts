import { describe, it, expect } from "vitest";

describe("MDX Utilities", () => {
  it.todo("getAllPosts returns sorted array of posts");
  it.todo("getPostBySlug returns post with content");
  it.todo("parses frontmatter correctly");
  it.todo("calculates reading time estimate");
  it.todo("handles missing slug gracefully");
});
