// Plan 04 fills in these tests
import { describe, it, expect } from "vitest";

describe("Dispatch", () => {
  it.todo("requires tracking number for parcel dispatch");
  it.todo("requires consignment number for pallet dispatch");
  it.todo("transitions packed to dispatched");
  it.todo("triggers cert pack generation on dispatch");
  it.todo("stores dispatch details on order");
});
